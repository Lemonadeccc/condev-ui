import { H1, H3Sub, Code } from "@/app/_components/typography";
import {
  CdAlertBasicExample,
  CdAlertToastExample,
  TailwindBasicExample,
  ModalAlertTailwindBasicExample,
  DrawerAlertTailwindBasicExample,
} from "./example";

<H1 subheading="提示组件，用于展示全局/局部状态信息，支持 info/success/warning/error/loading、关闭按钮与 Toast 进度条">Alert</H1>

{/* ### Tailwind 实现参考 */}

<H3Sub
  sub={<>如果你想自定义样式或不依赖现成组件，可以参考这个 Tailwind 实现。</>}
>
  {"Tailwind 实现参考"}
</H3Sub>
<TailwindBasicExample />

{/* ### 在弹层中使用 */}

<H3Sub sub={<>Alert 的布局可以复用在 Modal / Drawer 等弹层容器中。</>}>
  {"在弹层中使用"}
</H3Sub>
<ModalAlertTailwindBasicExample />
<DrawerAlertTailwindBasicExample />
