import { H1, H3Sub, Code } from "@/app/_components/typography";
import {
  BasicExample,
  CheckedExample,
  GroupExample,
  IndeterminateExample,
} from "./example";

<H1 subheading="">Checkbox</H1>

{/* ### 基本用法 */}

<H3Sub
  sub={
    <>
      <div className="mb-2">
        最简单的用法是直接渲染 <Code>CdCheckbox</Code>。你可以通过{" "}
        <Code>checked</Code> 控制选中状态，通过 <Code>disabled</Code> 禁用交互。
      </div>
      <div className="text-white/70 text-[13px] leading-6">
        注意：HTML 原生并没有 <Code>readonly</Code> 的多选框语义；这里的{" "}
        <Code>readonly</Code> 是组件层的约定，用于“展示为可读但不允许修改”。
      </div>
    </>
  }
>
  {"基本用法"}
</H3Sub>

<BasicExample />

{/* ### 受控用法 */}

<H3Sub
  sub={
    <>
      通过 <Code>checked</Code>（以及对应的 <Code>onChange</Code>
      ）可以将状态交给外部维护，适用于表单联动、批量操作等场景。
    </>
  }
>
  {"受控用法"}
</H3Sub>
<CheckedExample />

{/* ### 多选框组 */}

<H3Sub
  sub={
    <>
      将多个 <Code>CdCheckbox</Code> 放入 <Code>Checkbox.Group</Code>{" "}
      中即可形成多选框组。
      <div className="mt-2 text-white/70 text-[13px] leading-6">
        <Code>defaultValue</Code> 用于非受控默认值；<Code>value</Code> +{" "}
        <Code>onChange</Code> 用于受控模式。
      </div>
    </>
  }
>
  {"多选框 Group"}
</H3Sub>
<GroupExample />

{/* ### 半选 / 全选 */}

<H3Sub
  sub={
    <>
      通过 <Code>indeterminate</Code> 可展示“部分选中”的视觉状态，常用于“全选 /
      反选”场景。
      <div className="mt-2 text-white/70 text-[13px] leading-6">
        建议：当选中项数量为 0
        时取消半选；当选中项数量等于总数时取消半选并设置全选；其余情况设置半选。
      </div>
    </>
  }
>
  {"半选 / 全选"}
</H3Sub>
<IndeterminateExample />
