import {
  ImageTypography,
  AvatarM,
  CodeBlock,
  H1,
  Divide,
  H3Sub,
  Code,
} from "@/app/_components/typography";
import {
  BasicExample,
  CustomExample,
  StatusExample,
  PositionExample,
  UpdateMessageExample,
  CloseExample,
} from "./read";

<H1 subheading="">Toast</H1>

{/* ### 基本用法 */}

<H3Sub
  sub={
    <></>
}

> {"基本用法"}

</H3Sub>
<BasicExample />

{/* ### 不同类型 */}

<H3Sub
  sub={
    <>
      全局提示有 5 种不同的类型，分别为：<Code>info</Code>、<Code>success</Code>、
      <Code>warning</Code>、<Code>error</Code>、<Code>loading</Code>
    </>
  }
>
  {"不同类型"}
</H3Sub>
<StatusExample />

{/* ### 自定义组件 */}

<H3Sub
  sub={
    <>
      可以使用基本的<Code>createToastStore</Code>方法来自定义 toast 组件,
      这里我们定义另一种
    </>
  }
>
  {"自定义组件"}
</H3Sub>
<CustomExample />

{/* ### 位置 */}

<H3Sub
  sub={
    <>
      <Code>message</Code> 组件的位置可自定义，以下的 <Code>message</Code>
      我们定位在了右下角
    </>
  }
>
  {"位置"}
</H3Sub>

以下为配置 `ToastProvider` 的代码

- `bottom-28`(定向到底部)，`items-end pr-12`(定位到右边)，所以就是 `右下方`
- 并且 `direction` 设为 `bottom-to-top` ，所以就是从下往上弹出
- 最后 `stack` 设为 `true` ，并且我们使用 `stack` 模式

<CodeBlock
  code={`export const toastBottomStore = createToastStore({ maxCount: 6 });
 
<ToastProvider
    store={toastBottomStore}
    containerProps={{
      className: cs('w-full fixed bottom-12 flex flex-col items-end pr-12 pointer-events-none', \`\${TOAST_Z_INDEX}\`),
    }}
    direction="bottom-to-top"
/>`}
  language="tsx"
  className="mb-12"
/>

<PositionExample />

{/* ### 手动控制关闭 */}

<H3Sub
  sub={
    <>
      传入<Code>showClose</Code>可以让 message
      显示关闭按钮。点击关闭按钮可以手动关闭 message
    </>
  }
>
  {"手动控制关闭"}
</H3Sub>

<CloseExample />

{/* ### 更新内容 */}

<H3Sub
  sub={
    <>
      通过指定 <Code>id</Code>，可以更新已经存在的 <Code>message</Code>{" "}
      内容。注意， <Code>duration</Code> 值为 <Code>null</Code> 会取消当前
      <Code>message</Code> 消息自动关闭功能
    </>
  }
>
  {"更新内容"}
</H3Sub>

<UpdateMessageExample />
